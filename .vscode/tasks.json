{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "Jekyll: Serve (LiveReload)",
        "type": "shell",
        "command": "bundle exec jekyll serve --livereload",
        "options": {
            "cwd": "${workspaceFolder}"
        },
        "problemMatcher": [],
        "group": {
            "kind": "build",
            "isDefault": true
        }
    }
]
}
